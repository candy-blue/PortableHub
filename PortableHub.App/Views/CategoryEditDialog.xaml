<Window x:Class="PortableHub.App.Views.CategoryEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PortableHub.App.ViewModels"
        Title="分类管理" 
        Height="360" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BrushBackground}"
        Foreground="{DynamicResource BrushTextPrimary}">
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding IsEditMode, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse, FallbackValue='编辑分类'}"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />

        <!-- Form -->
        <StackPanel Grid.Row="1">
            <StackPanel>
                <TextBlock Text="分类名称" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,6" />
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" />
            </StackPanel>

            <StackPanel>
                <TextBlock Text="分类主题色" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,6" />
                <ItemsControl ItemsSource="{Binding AvailableColors}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Margin="0,0,8,8" Width="26" Height="26"
                                    Cursor="Hand"
                                    Command="{Binding DataContext.SelectColorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="13" Background="{Binding Converter={StaticResource ColorHexToBrushConverter}}"
                                                BorderThickness="2" BorderBrush="#FFFFFF" />
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="排序序号 (越小越靠前)" FontSize="12" Foreground="{DynamicResource BrushTextSecondary}" Margin="0,0,0,6" />
                <TextBox Text="{Binding SortOrder, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Width="100" HorizontalAlignment="Left" />
            </StackPanel>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Style="{StaticResource ModernButton}" Command="{Binding CancelCommand}" Width="80" />
            <Button Content="保存" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}" Width="80" Margin="10,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
