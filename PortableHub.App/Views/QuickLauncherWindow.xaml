<Window x:Class="PortableHub.App.Views.QuickLauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PortableHub.App.ViewModels"
        Title="快速启动" 
        Height="480" Width="680"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        PreviewKeyDown="Window_PreviewKeyDown"
        Deactivated="Window_Deactivated">

    <Border Background="{DynamicResource BrushSurface}" 
            BorderBrush="{DynamicResource BrushBorder}" 
            BorderThickness="1" 
            CornerRadius="12"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="6" Opacity="0.35" Color="#000000" />
        </Border.Effect>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Search Input (WinUI 3 AutoSuggestBox) -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <TextBox x:Name="SearchBox"
                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}"
                         FontSize="15"
                         Padding="36,10,12,10"
                         Height="46"
                         VerticalContentAlignment="Center" />
                <TextBlock Text="&#xE721;" Style="{StaticResource FluentIcon}" FontSize="14" Foreground="{DynamicResource BrushTextSecondary}" 
                           VerticalAlignment="Center" HorizontalAlignment="Left" Margin="12,0,0,0" IsHitTestVisible="False" />
                <TextBlock Text="输入软件名称、分类或标签..."
                           FontSize="14"
                           Foreground="{DynamicResource BrushTextTertiary}"
                           VerticalAlignment="Center"
                           Margin="38,0,0,0"
                           IsHitTestVisible="False"
                           Visibility="{Binding SearchQuery, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}" />
            </Grid>

            <!-- Results List -->
            <ListBox Grid.Row="1"
                     x:Name="ResultsListBox"
                     ItemsSource="{Binding Results}"
                     SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder" CornerRadius="6" Background="{TemplateBinding Background}" Padding="10,8">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource BrushCardHover}" />
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
                                            <Setter TargetName="ItemBorder" Property="BorderThickness" Value="1" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource BrushCardHover}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Icon -->
                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Background="{DynamicResource BrushBackground}" Margin="0,0,12,0">
                                <Image Source="{Binding IconPath, Converter={StaticResource StringToImageSourceConverter}}" Width="28" Height="28" Stretch="Uniform" />
                            </Border>

                            <!-- Name & Category -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource BrushTextPrimary}" />
                                    <Border Background="{DynamicResource BrushBadgeBg}" CornerRadius="4" Padding="6,1" Margin="8,0,0,0">
                                        <TextBlock Text="{Binding CategoryName}" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding ExePath}" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" 
                                           TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
                            </StackPanel>

                            <!-- Launch Hint -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Background="{DynamicResource BrushBadgeBg}" CornerRadius="3" Padding="6,2" Margin="0,0,6,0">
                                    <TextBlock Text="Enter" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextSecondary}" />
                                </Border>
                                <TextBlock Text="启动" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" VerticalAlignment="Center" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Bottom shortcuts info bar with WinUI 3 keycap badges -->
            <Border Grid.Row="2" BorderBrush="{DynamicResource BrushBorder}" BorderThickness="0,1,0,0" Padding="0,12,0,0" Margin="0,8,0,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{DynamicResource BrushBadgeBg}" CornerRadius="3" Padding="5,1" Margin="0,0,4,0">
                        <TextBlock Text="&#xE70E; &#xE70D;" Style="{StaticResource FluentIcon}" FontSize="10" Foreground="{DynamicResource BrushTextSecondary}" />
                    </Border>
                    <TextBlock Text="移动" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" Margin="0,0,14,0" VerticalAlignment="Center" />

                    <Border Background="{DynamicResource BrushBadgeBg}" CornerRadius="3" Padding="6,1" Margin="0,0,4,0">
                        <TextBlock Text="Enter" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextSecondary}" />
                    </Border>
                    <TextBlock Text="启动" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" Margin="0,0,14,0" VerticalAlignment="Center" />

                    <Border Background="{DynamicResource BrushBadgeBg}" CornerRadius="3" Padding="6,1" Margin="0,0,4,0">
                        <TextBlock Text="Esc" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextSecondary}" />
                    </Border>
                    <TextBlock Text="关闭" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" Margin="0,0,14,0" VerticalAlignment="Center" />

                    <Border Background="{DynamicResource BrushBadgeBg}" CornerRadius="3" Padding="6,1" Margin="0,0,4,0">
                        <TextBlock Text="Ctrl + 1..9" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource BrushTextSecondary}" />
                    </Border>
                    <TextBlock Text="快速直达" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
