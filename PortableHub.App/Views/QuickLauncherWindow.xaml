<Window x:Class="PortableHub.App.Views.QuickLauncherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PortableHub.App.ViewModels"
        Title="快速启动" 
        Height="460" Width="680"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        PreviewKeyDown="Window_PreviewKeyDown"
        Deactivated="Window_Deactivated">

    <Border Background="{DynamicResource BrushSurface}" 
            BorderBrush="{DynamicResource BrushBorder}" 
            BorderThickness="1" 
            CornerRadius="12"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.3" Color="#000000" />
        </Border.Effect>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Search Input -->
            <Grid Grid.Row="0" Margin="0,0,0,12">
                <TextBox x:Name="SearchBox"
                         Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}" 
                         Style="{StaticResource ModernTextBox}"
                         FontSize="16"
                         Padding="12,10"
                         Height="46"
                         VerticalContentAlignment="Center" />
                <TextBlock Text="🔍 输入软件名称、分类或标签..."
                           FontSize="15"
                           Foreground="{DynamicResource BrushTextTertiary}"
                           VerticalAlignment="Center"
                           Margin="14,0,0,0"
                           IsHitTestVisible="False"
                           Visibility="{Binding SearchQuery, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}" />
            </Grid>

            <!-- Results List -->
            <ListBox Grid.Row="1"
                     x:Name="ResultsListBox"
                     ItemsSource="{Binding Results}"
                     SelectedIndex="{Binding SelectedIndex, Mode=TwoWay}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,2" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder" CornerRadius="8" Background="{TemplateBinding Background}" Padding="10,8">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource BrushCardHover}" />
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource BrushAccent}" />
                                            <Setter TargetName="ItemBorder" Property="BorderThickness" Value="1" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource BrushCardHover}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Icon -->
                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="8" Background="{DynamicResource BrushBackground}" Margin="0,0,12,0">
                                <Image Source="{Binding IconPath, Converter={StaticResource StringToImageSourceConverter}}" Width="28" Height="28" Stretch="Uniform" />
                            </Border>

                            <!-- Name & Category -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource BrushTextPrimary}" />
                                    <Border Background="{DynamicResource BrushBorder}" CornerRadius="4" Padding="5,1">
                                        <TextBlock Text="{Binding CategoryName}" FontSize="11" Foreground="{DynamicResource BrushTextSecondary}" />
                                    </Border>
                                </StackPanel>
                                <TextBlock Text="{Binding ExePath}" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" 
                                           TextTrimming="CharacterEllipsis" Margin="0,2,0,0" />
                            </StackPanel>

                            <!-- Launch Hint -->
                            <TextBlock Grid.Column="2" Text="Enter 启动" FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Bottom shortcuts info -->
            <Grid Grid.Row="2" Margin="0,10,0,0">
                <TextBlock Text="↑ ↓ 选择   Enter 启动   Esc 关闭   Ctrl+1..9 快速选择" 
                           FontSize="11" Foreground="{DynamicResource BrushTextTertiary}" />
            </Grid>
        </Grid>
    </Border>
</Window>
